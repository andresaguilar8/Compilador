hacer

	constructor


verificar

    tiene q haber un solo metodo main o si hay mas saber cual es el main
	test35

preguntar 



	si una clase no implementa todos los metodos de una interface que implementa, el error esta en el token de la clase? creo que dijeron
	que en el implements

	los metodos estaticos se heredan? supongo que si porque sino se podria "redefinir" un metodo estatico incorrectamente


    herencia circular detectar uno solo (cualquiera) el del extends dijo gotti



