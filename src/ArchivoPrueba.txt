class A {

    void metodo() {
        var z = 2;
        z = 3;
        if (z) {
            z = false;
        }
        while (x) {
            ;
        }

        if (this.metodo() > 5)
            return new Clase();
    }

}
